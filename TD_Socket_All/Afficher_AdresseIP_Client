printf("message du client %s/%d: %d\n", inet_ntoa(infoClient.sin_addr), infoClient.sin_port, valRec);
                          **            ******************************
Pour affichre l'adresse IP du Client
